import { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["i.pinimg.com", "github.com"], // Added GitHub domain for compatibility
  },
};

export default nextConfig;

